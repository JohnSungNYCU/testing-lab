name: CI

on: [push]
jobs:
  coverate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install vitest
        working-directory: backend
        run: npm install 

      - name: Coverage Test
        working-directory: backend
        run: npm run test
  
